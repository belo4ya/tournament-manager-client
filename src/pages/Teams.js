import React from 'react';
import ProfileTeam from "../components/ProfileTeam";

const Teams = () => {
    return (
        <div>
            <ProfileTeam />
        </div>
    );
};

export default Teams;