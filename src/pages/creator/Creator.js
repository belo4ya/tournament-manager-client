import React, {useEffect} from 'react';

const Creator = () => {

    useEffect(() => {
        document.title = 'Об авторе'
    }, [])

    return (
        <div>

        </div>
    );
};

export default Creator;